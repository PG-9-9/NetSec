name: workflow

on:
    push:
        branches:
        - main
        paths-ignore:
        - 'README.md'

jobs:
    integeration:
        name: Continuous Integration
        runs-on: ubuntu-latest
        steps:
            - name: Checkout Code
              uses: actions/checkout@v3

            - name: Lint Code
              run: echo "Linting Code Repo"

            - name: Run Unit Tests
              run: echo "Running Unit Tests"